<div class="mt4">
    <div class="row">
        <div class="col-sm-6 col-lg-3">
            {{#each sims}}
            <div class="card" style="width: 18rem;">
  <img src="{{this.image}}" alt=" ">
  <div class="card-body">
    <h5 class="">{{this.phoneNumber}}</h5>
    <p class="card-text">{{this.price}}</p>
    <a href="#" class="btn btn-primary">Mua Sim</a>
  </div>
</div>
{{/each}}
        </div>
    </div>
</div>